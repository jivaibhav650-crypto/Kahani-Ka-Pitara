function match(q,x){q=String(q||'').toLowerCase().trim();if(!q)return true;const raw=`${x.title||''} ${x.excerpt||''} ${x.content||''} ${x.category||''}`.toLowerCase();const tr=autoTrans(raw);const words=q.split(/\s+/);const aliases=words.map(w=>romanAliases[w]||w);const aliasText=aliases.join(' ');return raw.includes(q)||raw.includes(aliasText)||tr.includes(q)||tr.includes(aliasText)||words.every((w,i)=>raw.includes(w)||tr.includes(w)||raw.includes(aliases[i]));}
